using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CapsulePlacer : MonoBehaviour
{
    private void SomeMethod()
    {
        Capsule someCapsule = null;
        
        //someCapsule.SetupCapsule(this)
        //    .PlaceCapsule(Vector3.zero)
        //    .LaunchAnimation()
        //    .ScoresReceived += 

        // Сделать интерфейс и передать объект CapsulePlacer под этим интерфейсом
        // В интерфейсе два метода
        // 1) возврат капсулы
        // 2) сообщение что капсулу подобрал игрок
    }

    public void ReturnCaplsule(Capsule capsule)
    {

    }

    public void SetCapsuleOnPlatform(ReusablePlatform platform)
    {
        // Проверка
        //if ()
        //{

        //}
    }
}
